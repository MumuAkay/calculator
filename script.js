let operation

function add(a,b) {
    operation = a + b;
    return operation
}

function substract(a,b) {
    operation = a - b;
    return operation
}

function divide(a,b) {
    operation = a / b;
    return operation
}

function multiply(a,b) {
    operation = a * b;
    return operation
}